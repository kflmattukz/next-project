const LOGIN = "/login";
const DASHBOARD = "/doshboard";
const USERS = "/user";

const PROTECTED_PATH = [DASHBOARD, USERS];

const AUTH_PATH = [LOGIN];

export { PROTECTED_PATH, AUTH_PATH, LOGIN, DASHBOARD, USERS };
